{
  "name": "activites-jeunesse",
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build",
    "start": "node --no-warnings backend/index.js",
    "setup": "cd backend && npm install && cd ../frontend && npm install && npm run build"
  }
}
